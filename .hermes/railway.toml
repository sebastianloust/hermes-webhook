[build]
builder = "dockerfile"
dockerfile = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn -w 4 -b 0.0.0.0:$PORT webhook_flask_railway:app'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
